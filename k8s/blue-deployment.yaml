apiVersion: apps/v1
kind: Deployment
metadata:
  name: sentiment-blue-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      slot: blue
  template:
    metadata:
      labels:
        slot: blue
    spec:
      containers:
        - name: sentiment-api
          image: alizaibdocker/sentiment-api:unstable
          ports:
            - containerPort: 5000
          volumeMounts:
            - name: logs-volume
              mountPath: /app/logs
      volumes:
        - name: logs-volume
          persistentVolumeClaim:
            claimName: sentiment-logs-pvc
